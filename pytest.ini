[pytest]
addopts =
    --cov djcelery_email
    --cov-report html
    --cov-config pytest.ini

python_paths =
    test_environment

DJANGO_SETTINGS_MODULE = test_env_app.settings

# coverage
[run]
source = djcelery_email

[html]
directory = var/report/coverage
